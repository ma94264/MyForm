<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"

	xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans.xsd
	http://www.springframework.org/schema/security
    http://www.springframework.org/schema/security/spring-security.xsd">

	<!--enable @preauthorize and @postauthorize -->
	<global-method-security pre-post-annotations="enabled" />

	<http>
		<intercept-url pattern="/home**" access="hasRole('STUDENT')" />
		<intercept-url pattern="/tables**" access="hasRole('STUDENT')" />
		<form-login login-page='/login.neil' default-target-url="/home.neil"
			always-use-default-target='true' />
		<csrf disabled="true" />
		<logout />
	</http>

	<beans:bean id="customAuthenticationProvider"
		class="com.neil.spring.CustomAuthenticationProvider">
	</beans:bean>
	<authentication-manager>
		<authentication-provider>
			<jdbc-user-service data-source-ref="dataSource"
				authorities-by-username-query="select username,ROLE from user_roles where username=?"
				users-by-username-query="select username,password,enabled from users where username=?" />
		</authentication-provider>
		<authentication-provider ref="customAuthenticationProvider" />
	</authentication-manager>
	<!-- <authentication-manager> <authentication-provider> <user-service> <user 
		name="jimi" password="jimispassword" authorities="ROLE_USER, ROLE_ADMIN" 
		/> <user name="bob" password="bobspassword" authorities="ROLE_USER" /> </user-service> 
		</authentication-provider> </authentication-manager> -->
</beans:beans>